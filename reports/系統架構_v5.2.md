# Quantum Finance 系統架構與旗艦開發標準 (v5.2)

## 🏛️ 系統憲法：核心禁區 (Holy Forbidden Zone)
本系統的核心主權定義在以下兩大組件，為系統運行的最高基準：
1.  **`.env`**：全系統環境變數、API 金鑰與 27+ 維 DDD 路徑體系。
2.  **`scripts/core/`**：包含數據契約、資料庫治理、API 通訊與模型元數據核心。

> [!IMPORTANT]
> **嚴禁變動原則**：未來開發任何外圍模組（如數據抓取、特徵工程、模型推論）時，**嚴禁** 為了適配新需求而改動 `core/` 或 `.env`。所有外圍程式必須主動適配核心介面。

---

## 📡 核心組件演進編年史 (The Sovereign Chronicle)
本節保留了核心配置與治理邏輯的所有歷史變遷，作為未來所有修改的權威參考基準。

### 1. 核心定義與主權矩陣的演進 (Historical Core Definitions)
*   **v4.2 (2026-05-12) - 旗艦主權版**: 確立以 `PROJECT_ROOT` 為基準的 27 維自愈路徑，並注入「混合日誌」與「維運矩陣」標準。
*   **v4.0 (2026-05-11) - Full DDD 版**: 首次引入 RAW_DATA, FEATURE_STORE, WEIGHTS, SCALERS 等細分目錄，對齊領域驅動設計（DDD）。
*   **v3.10 (2026-05-11) - 路徑治理版**: 整合 22 維全路徑體系，結束了散亂路徑的時代。
*   **v2.0 (2026-05-10) - 核心奠基版**: 首次規範資料庫連線憑證與 FinMind API v4 Token 注入標準。

### 2. 舊版維運範例保留 (Legacy Operational Examples)
為了回溯兼容與邏輯參考，特保留以下歷史維運指令：
*   **舊版數據初始化 (v2.0)**: `$ python scripts/core/db_utils.py --init` (現已併入 `data_schema.py`)
*   **早期路徑校驗 (v3.10)**: `$ python scripts/core/path_setup.py --check-all`
*   **舊版 Token 驗證**: `$ python scripts/maintenance/check_finmind_quota.py` (現已整合至 `finmind_client.py` 診斷模組)

### 3. 憲法級參考：核心路徑定義說明 (Sovereign Path Map)
*   **[Data Ingestion Layer]**: 確保原始數據 (RAW) 在 Ingestion 層被正確隔離，避免污染。
*   **[Feature Store Layer]**: 特徵工程產出具備獨立空間，防止訓練推理時的「特徵偏移」。
*   **[Learning & Prediction Layer]**: 模型權重與預測結果必須嚴格存放於指定路徑，禁止隨意散落在 scripts 目錄。

---

## 🏁 旗艦工程終極驗收 (Final Acceptance - 2026-05-11)
本系統已完成「數據治權與現代化重構」，達成以下 PERFECT 狀態：

### 1. 地基完備：數據契約主權
*   **數據契約**：`data_schema.py v1.7` 達成 1:1 API 鏡像對齊。
*   **高精度治理**：全系統數值欄位統一採用 `NUMERIC(20, 6)`。

### 2. 觀測完備：雙軌日誌體系 (Hybrid Mode)
所有核心操作必須同時記錄於：
*   **生命週期日誌 (`pipeline_execution_log`)**。
*   **專項審計日誌 (`data_audit_log`)**。

### 3. 文檔完備：極致維運矩陣 (Operational Matrix)
`core/` 下每一支程式均包含：
*   **完整修訂歷程**：保留所有過往變更紀錄。
*   **維運指令矩陣**：包含「個股、單一表、單一個股所有表、核心股全量強制更新」等情境。

---
**本文件為系統開發之最高指導原則。**
**狀態：PERFECT**
