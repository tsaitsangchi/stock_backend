# Quantum Finance 系統架構與旗艦開發標準 (v5.2)

## 🏛️ 系統憲法：核心禁區 (Holy Forbidden Zone)
本系統的核心主權定義在以下兩大組件，為系統運行的最高基準：
1.  **`.env`**：全系統環境變數、API 金鑰與 22+ 維 DDD 路徑體系。
2.  **`scripts/core/`**：包含數據契約、資料庫治理、API 通訊與模型元數據核心。

> [!IMPORTANT]
> **嚴禁變動原則**：未來開發任何外圍模組（如數據抓取、特徵工程、模型推論）時，**嚴禁** 為了適配新需求而改動 `core/` 或 `.env`。所有外圍程式必須主動適配核心介面。

---

## 📡 .env 核心定義編年史 (Environmental Governance & Chronicle)
本節記錄了系統「血管地圖」的所有歷史定義與演進，作為未來所有修改的權威參考。

### 1. 核心定義與主權矩陣 (Core Definitions)
*   **基礎設施與金鑰 (Infrastructure)**：確保資料庫連線安全性與外部 API (FinMind, FRED, Gemini) 的認證主權。
*   **DDD 全路徑體系 (Domain-Driven Path Map)**：
    *   **Data Ingestion**: 確保原始數據在 Ingestion 層被正確物理隔離。
    *   **Feature Store**: 確保特徵工程產出具備獨立的儲存空間。
    *   **Learning Layer**: 模型權重、Scalers 與 MLflow 紀錄的專屬區域。
*   **全系統路徑治理 (Sovereign Path Governance)**：
    *   以 `PROJECT_ROOT` 為唯一物理基準，確保全系統程式具備 100% 的路徑移植性與自癒能力。

### 2. 歷史維運範例 (Historical Operational Examples)
*   **數據全同步**：`$ python scripts/ingestion/template_fetcher.py --universe core --all_datasets`
*   **系統級健康診斷**：`$ python scripts/maintenance/check_system_health.py`
*   **數據契約重鑄**：`$ python scripts/core/data_schema.py --init --force`
*   **標的一鍵遷移**：`$ python scripts/core/migrate_stocks_config.py`

### 3. 全修訂歷程 (Full Revision History)
*   **v4.2 (2026-05-12)**: [旗艦] 注入詳細核心定義與範例矩陣，確立為 v5.2 憲法基準。
*   **v4.1 (2026-05-12)**: [標準] 注入路徑治理與 DDD 邊界說明。
*   **v4.0 (2026-05-11)**: [架構] 升級至 Full DDD 版。補齊數據細分 (RAW_DATA, FEATURE_STORE) 與模型資產目錄。
*   **v3.10 (2026-05-11)**: [治理] 整合 22 維全路徑體系，確立 PROJECT_ROOT 為唯一基準。
*   **v2.0 (2026-05-10)**: [核心] 加入 FinMind API v4 Token 與資料庫憑證。

---

## 🏁 旗艦工程終極驗收 (Final Acceptance - 2026-05-11)
本系統已於 2026-05-11 完成「數據治權與現代化重構」，達成以下 PERFECT 狀態：

### 1. 地基完備：數據契約主權
*   **數據契約**：`data_schema.py v1.7` 達成與 FinMind API 1:1 的實體鏡像對齊。
*   **高精度治理**：全系統數值欄位統一採用 `NUMERIC(20, 6)`，確保金融計算精度。

### 2. 觀測完備：雙軌日誌體系 (Hybrid Mode)
所有核心操作必須同時記錄於：
*   **生命週期日誌 (`pipeline_execution_log`)**：監控任務耗時與成功狀態。
*   **專項審計日誌 (`data_audit_log`)**：精確記錄數據異動（如 API 抓取筆數、Schema 變更）。

### 3. 文檔完備：極致維運矩陣 (Operational Matrix)
`core/` 下每一支程式均遵循旗艦開發規範，必須包含：
*   **完整修訂歷程**：保留所有過往版本的變更紀錄。
*   **維運指令矩陣**：完整定義以下四大維運情境的範例指令：
    *   **個股 (Individual Stock)**
    *   **單一表 (Single Table)**
    *   **單一個股所有表 (Single Stock All Tables)**
    *   **所有核心股與所有 Table 強制更新 (All Core & All Tables Force Update)**

### 4. 連通完備：供應鏈自動化
*   **環境自癒**：`finmind_client v4.24` 已整合 `load_dotenv()`，實現 Token 自動注入與多端點探測。
*   **主權遷移**：系統已全面進入「資料庫治權時代」，棄用硬編碼配置。

---
**本文件為系統開發之最高指導原則。**
**狀態：PERFECT**
